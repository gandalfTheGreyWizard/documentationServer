services:
  documentation_server:
    env_file: .env
    build:
      context: .
      dockerfile: Dockerfile
    restart: unless-stopped
    ports:
      - ${PORT}:8080
    volumes:
      - ${DOCUMENTATION_PATH}:/usr/src/app/docs
